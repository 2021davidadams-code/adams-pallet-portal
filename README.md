# Adams Pallet Portal Starter

## Run locally
1. Install Node.js
2. Run `npm install`
3. Copy `.env.local.example` to `.env.local`
4. Add your Supabase keys
5. Run `npm run dev`

## Deploy
Push to GitHub, then import into Vercel and add the same environment variables.
